# Default config for llm_resoning.
# Fill this with your local model path before running real inference.
method: llm_resoning
# model_path: /home/liujn/workspace/models/qwen/pretrained/Qwen2.5-1.5B  # TODO: set local model path, for example /path/to/Qwen3-4B
# model_name: qwen2.5_1.5b
model_path: /home/fit/renjuliuji/WORK/liujn/models/qwen/finetuned/Qwen3-4B-Instruct-2507
model_name: qwen3_4b_instruct

dataset_name: aime24
dataset_path: /home/fit/renjuliuji/WORK/liujn/datasets/benchmarks/math/AIME24  # TODO: set dataset path
dataset_type: auto
question_field:  # Optional override. Leave empty to use dataset_name defaults.
answer_field:  # Optional override. Leave empty to use dataset_name defaults.
# Number of problems to run this time. Leave empty to run the whole dataset.
num_examples: 1
# Optional comma-separated dataset ids to run, for example: 60,72
example_ids:

prompt_template_path: templates/plan_prefix_prompt.txt

output_root: /home/fit/renjuliuji/WORK/liujn/experiments/inference
branch_num: 2
temperature: 0.7
top_p: 0.9
top_k: -1
max_new_tokens: 32768
seed: 42

tensor_parallel_size: 1
gpu_memory_utilization: 0.9
max_model_len:
trust_remote_code: true
